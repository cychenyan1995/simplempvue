<view class="i-class i-badge i-badge-{{align}}">
    <slot></slot>
    <view class="i-badge-dot" wx:if="{{ dot }}"></view>
    <view class="i-badge-count i-class-alone" wx:elif="{{ count !== 0 }}">{{ finalCount }}</view>
</view>
